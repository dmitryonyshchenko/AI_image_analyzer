# ── Local development ────────────────────────────────────────────────────────
# 1. Copy this file:  cp .env.example .env
# 2. Fill in the key for your chosen AI provider (see below)
# 3. Run: python app.py

# ── AI provider ───────────────────────────────────────────────────────────────
# Which AI backend to use. Must match a file in ai_backends/<provider>.py
# Available: gemini_api (default, Google Gemini via SDK with enforced JSON schema)
# To add a new backend: create ai_backends/my_provider.py, then set AI_PROVIDER=my_provider
AI_PROVIDER=gemini_api

# ── Gemini (default) ──────────────────────────────────────────────────────────
# Get a free key at https://aistudio.google.com/apikey
# IMPORTANT: never paste this key into chat — add it here directly in your editor
GEMINI_API_KEY=your_api_key_here

# ── Hosted environments (Render, Railway, Fly.io, etc.) ──────────────────────
# Do NOT use this file on hosted environments.
# Set AI_PROVIDER and the relevant API key as environment variables
# in the hosting dashboard instead.
# This file (.env) is gitignored and must never be committed with a real key.
